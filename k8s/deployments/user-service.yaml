apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: cloudmart-prod
  labels:
    app: user-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: user-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: user-service
    spec:
      serviceAccountName: user-service-sa
      containers:
      - name: user-service
        image: 145023091806.dkr.ecr.ap-south-1.amazonaws.com/cloudmart/user-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8003
        env:
        - name: PORT
          value: "8003"
        - name: AWS_REGION
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: AWS_REGION
        - name: AWS_DEFAULT_REGION
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: AWS_REGION
        - name: DB_BACKEND
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: USER_DB_BACKEND
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: DB_PORT
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: DB_NAME
        - name: DB_SSLMODE
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: DB_SSLMODE
        - name: DB_USER
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: DB_USER
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: cloudmart-secrets
              key: DB_PASSWORD
        - name: DB_SECRET_NAME
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: DB_SECRET_NAME
        - name: JWT_SECRET_NAME
          valueFrom:
            configMapKeyRef:
              name: cloudmart-config
              key: JWT_SECRET_NAME
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 256Mi
        readinessProbe:
          httpGet:
            path: /ready
            port: 8003
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 8003
          initialDelaySeconds: 10
          periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: cloudmart-prod
spec:
  selector:
    app: user-service
  ports:
  - port: 8003
    targetPort: 8003
  type: ClusterIP
